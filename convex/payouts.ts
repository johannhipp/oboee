/**
 * Automated payout execution is deliberately outside the testnet MVP.
 * Payout ledger rows remain accounting records and must never be represented
 * as on-chain settlement without a verified transfer implementation.
 */
export const automatedPayoutsEnabled = false;
